--- a/html/includes/print-menubar.php
+++ b/html/includes/print-menubar.php
@@ -356,8 +356,9 @@
 }
 
 # Copy the variable so we can use $used_sensors later in other parts of the code
-$menu_sensors = $used_sensors;
-
+if (isset($used_sensors)) {
+	$menu_sensors = $used_sensors;
+}
 ?>
 
         <li class="dropdown">
